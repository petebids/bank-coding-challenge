# Shared secret between the mock vendor API and the Temporal worker's activity.
# Used by docker-compose (mock-vendor-api, temporal-worker) and, for local `next dev`,
# by the temporal-worker process only — the browser and Next.js app never see it.
VENDOR_API_KEY=dev-secret-key
